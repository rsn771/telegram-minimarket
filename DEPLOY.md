# 🚀 Деплой на Vercel

## Способ 1: Через веб-интерфейс Vercel (Рекомендуется)

### Шаг 1: Подготовка репозитория

1. Убедитесь, что все изменения закоммичены:
   ```bash
   git add .
   git commit -m "Подготовка к деплою на Vercel"
   ```

2. Загрузите код на GitHub/GitLab/Bitbucket:
   ```bash
   git push origin main
   ```
   
   Если у вас еще нет удаленного репозитория:
   ```bash
   # Создайте репозиторий на GitHub
   # Затем выполните:
   git remote add origin https://github.com/ваш-username/telegram-minimarket.git
   git push -u origin main
   ```

### Шаг 2: Деплой через Vercel Dashboard

1. **Откройте [vercel.com](https://vercel.com)** и войдите через GitHub/GitLab/Bitbucket

2. **Нажмите "Add New Project"** (Добавить новый проект)

3. **Импортируйте ваш репозиторий:**
   - Найдите ваш репозиторий `telegram-minimarket`
   - Нажмите "Import"

4. **Настройки проекта:**
   - **Framework Preset:** Next.js (определится автоматически)
   - **Root Directory:** `./` (оставьте по умолчанию)
   - **Build Command:** `npm run build` (по умолчанию)
   - **Output Directory:** `.next` (по умолчанию)
   - **Install Command:** `npm install` (по умолчанию)

5. **Переменные окружения (если нужны):**
   - Пока можно оставить пустым
   - Если позже понадобятся API ключи, добавьте их здесь

6. **Нажмите "Deploy"**

7. **Дождитесь завершения деплоя** (обычно 1-3 минуты)

8. **Получите URL вашего приложения:**
   - После деплоя вы получите URL вида: `https://telegram-minimarket-xxxxx.vercel.app`
   - Этот URL можно использовать в BotFather для создания Mini App

### Шаг 3: Настройка домена (опционально)

1. В настройках проекта на Vercel перейдите в "Domains"
2. Добавьте свой домен (если есть)
3. Следуйте инструкциям для настройки DNS

---

## Способ 2: Через Vercel CLI

### Шаг 1: Установка Vercel CLI

```bash
npm i -g vercel
```

### Шаг 2: Вход в Vercel

```bash
vercel login
```

Следуйте инструкциям для входа через браузер.

### Шаг 3: Деплой

```bash
# Из корневой директории проекта
cd /Users/artemluzanin/Desktop/telegram-minimarket

# Первый деплой (production)
vercel

# Или для preview деплоя
vercel --prod
```

### Шаг 4: Следуйте инструкциям CLI

CLI задаст несколько вопросов:
- **Set up and deploy?** → `Y`
- **Which scope?** → Выберите ваш аккаунт
- **Link to existing project?** → `N` (для первого раза)
- **Project name?** → `telegram-minimarket` (или любое другое)
- **Directory?** → `./` (нажмите Enter)
- **Override settings?** → `N` (нажмите Enter)

После завершения вы получите URL вашего приложения.

---

## 🔄 Обновление приложения

После каждого изменения в коде:

### Если используете веб-интерфейс:
- Просто сделайте `git push` - Vercel автоматически задеплоит изменения

### Если используете CLI:
```bash
vercel --prod
```

---

## ✅ Проверка деплоя

1. Откройте URL вашего приложения в браузере
2. Убедитесь, что приложение загружается корректно
3. Проверьте консоль браузера на наличие ошибок
4. Убедитесь, что стили применяются правильно

---

## 🔧 Настройка для Telegram Mini App

После успешного деплоя:

1. Скопируйте URL вашего приложения (например: `https://telegram-minimarket-xxxxx.vercel.app`)

2. Откройте [@BotFather](https://t.me/BotFather) в Telegram

3. Создайте Mini App:
   ```
   /newapp
   ```
   - Выберите вашего бота
   - Укажите название
   - Укажите описание
   - Загрузите иконку (512x512px PNG)
   - **Вставьте URL вашего Vercel приложения**
   - Загрузите скриншоты (2-5 штук)
   - Укажите короткое название

4. Протестируйте:
   - Откройте вашего бота
   - Нажмите на кнопку меню
   - Выберите ваше Mini App

---

## 🐛 Решение проблем

### Проблема: Стили не применяются
**Решение:** Убедитесь, что `tailwind.config.js` правильно настроен и все файлы закоммичены.

### Проблема: Ошибка при сборке
**Решение:** 
- Проверьте логи сборки в Vercel Dashboard
- Убедитесь, что все зависимости указаны в `package.json`
- Попробуйте очистить кеш: Settings → Clear Build Cache

### Проблема: Telegram не открывает приложение
**Решение:**
- Убедитесь, что URL начинается с `https://`
- Проверьте, что приложение доступно из браузера
- Убедитесь, что CSP заголовки настроены правильно в `next.config.ts`

### Проблема: Приложение работает локально, но не на Vercel
**Решение:**
- Проверьте переменные окружения
- Убедитесь, что все файлы закоммичены
- Проверьте логи сборки в Vercel Dashboard

---

## 📝 Полезные команды

```bash
# Просмотр информации о проекте
vercel ls

# Просмотр логов
vercel logs

# Удаление проекта
vercel remove

# Просмотр информации о деплое
vercel inspect
```

---

## 🎉 Готово!

После успешного деплоя ваше приложение будет доступно по HTTPS и готово к использованию в Telegram Mini App!
